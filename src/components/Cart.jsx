import React from 'react'
import { useContext } from 'react'

const Cart = () => {
    // const cart = useContext(CartContext)
    // console.log(cart)
    return (
        <div>Cart</div>
    )
}

export default Cart